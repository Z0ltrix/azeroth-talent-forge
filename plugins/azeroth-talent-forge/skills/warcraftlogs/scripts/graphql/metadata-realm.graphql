query MetadataRealm($region: String!, $slug: String!) {
  worldData {
    server(region: $region, slug: $slug) {
      id
      name
      normalizedName
      slug
      region
      subregion
    }
  }
}
