query MetadataGame($abilityLimit: Int, $abilityPage: Int) {
  gameData {
    classes { id name slug specs { id name slug } }
    affixes { id name }
    abilities(limit: $abilityLimit, page: $abilityPage) { id name }
  }
}
